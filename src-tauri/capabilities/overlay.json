{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-capability",
  "description": "permissions for the area-selection overlay window",
  "windows": ["overlay-*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:webview:default",
    "store:default"
  ]
}
