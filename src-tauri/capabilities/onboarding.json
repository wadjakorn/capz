{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "onboarding-capability",
  "description": "permissions for first-launch onboarding window",
  "windows": ["onboarding"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-close",
    "core:webview:default",
    "store:default"
  ]
}
