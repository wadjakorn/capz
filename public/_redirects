# Cloudflare Pages redirects (capz-web deployment only).
# The root path now serves the marketing landing (merged in from the former
# capz-site Worker); the browser editor lives at /paste. No root redirect.
# Ignored by the Tauri desktop build — it serves its own bundle and never
# routes through Cloudflare.
