# Node
node_modules
node_modules/
.pnpm-store/
.next/
out/
dist/
*.tsbuildinfo

# Rust / Tauri
src-tauri/target/
src-tauri/gen/schemas/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp

# Env
.env
.env.local
.env*.local

# Cloudflare worker (worker/)
.wrangler/
.dev.vars

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Tauri build artifacts
src-tauri/Cargo.lock.bak

# Claude
.claude/tasks/
.claude/worktrees/

# Ladle
build/

# Playwright
e2e/test-results/
e2e/playwright-report/
e2e/.cache/
playwright/.cache/
.playwright-mcp/

# Local capture/debug artifacts
hud-default.png

# Agent scratch workspace (SDD ledger, briefs, review packages)
.superpowers/

# Local-only pnpm shim used when building on the Mac over mac-sync
# (verifyDepsBeforeRun:false). It must never reach the repo: committing it turns
# the project into a pnpm workspace and breaks the Cloudflare Pages install.
pnpm-workspace.yaml
